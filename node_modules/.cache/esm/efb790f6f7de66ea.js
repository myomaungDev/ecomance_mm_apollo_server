let gql;_f7d‍.x([["default",()=>_f7d‍.o]]);_f7d‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

_f7d‍.d(gql`
	extend type Query {
		all_categories: [Category!]
	}

	type Category {
		name: String!
		updatedAt: String
		createdAt: String
		img: String
		id: ID
	}

	extend type Mutation {
		add_category(name: String, img: String): ResponseCategory! @isSuperAdmin
		update_category(id: ID, name: String, img: String): ResponseCategory! @isSuperAdmin
		delete_category(id: ID): ResponseCategory! @isSuperAdmin
	}

	type ResponseCategory {
		message: String!
		status: Boolean
	}
`);
